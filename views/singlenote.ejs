<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 10px;
      }
      nav {
        display: flex;
        width: 100%;
        align-items: center;
        justify-content: space-around;
      }

      .post-card {
        border: 2px solid black;
        border-radius: 10px;
        box-shadow: 0px 0px 5px black;
        padding: 10px;
      }
      .post-container {
        display: flex;
        gap: 20px;
      }
      .login-details {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 5px;
      }
    </style>
  </head>
  <body>
    <button style="background-color: blue">
      <a href="/home" style="color: white">back</a>
    </button>
    <%if(typeof post !== 'undefined'){%>
    <div class="post-card">
      <img src="/uploads/<%=post.file%>" alt="image" width="200px" />
      <p>Title :-<%=post.title%></p>
      <p>Content :- <%=post.content%></p>
      <button style="background-color: orange">
        <a href="/notes/<%=post.notesid%>/delete" style="color: white"
          >Delete</a
        >
      </button>
    </div>
    <% }else{%>
    <p>Select a post</p>
    <% }%>
  </body>
</html>
